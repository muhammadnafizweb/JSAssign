// Soluation-01

let carName = "Volvo";
console.log(carName); // Output: Volvo

// Soluation-02

let firstName = "John", lastName = "Doe", age = 35;


// Soluation-03

x = 10;
y = 5;
x *= y; // Equivalent to x = x * y
console.log(x); // Output: 50


// Soluation-04

let length = 16; // data type: Number (integer)
let lastName1 = "Johnson"; // data type: String

const x = {
  firstName: "John",  
  lastName: "Doe"
}; // data type: Object


// Soluation-05

function myFunction() {
    alert("Hello World!");
  }
  
  // Call the function
  myFunction();


// Soluation-06

// Create the object
const person = {
    name: "John",
    age: 50
  };
  
  // Access the object and alert the message
  alert(person.name + " is " + person.age);


// soluation-07

{/* <button onclick="showMessage()">Click me.</button>

<script>
  function showMessage() {
    alert("Button clicked!");
  }
</script> */}


// soluation-08

// 1. Alert the number of items in an array:

const cars = ["Volvo", "Jeep", "Mercedes"];
alert(cars.length);

// 2. Change the first item of Brand to "Ford":

const Brand = ["Volvo", "Jeep", "Mercedes"];
Brand[0] = "Ford";
console.log(Brand);


// soluation-09

// 1. Use the correct Math method to create a random number:

let randomNumber = Math.random();
console.log(randomNumber); // Output: A random number between 0 and 1

// 2. Use the correct Math method to return the largest number of 10 and 20:

let largestNumber = Math.max(10, 20);
console.log(largestNumber); // Output: 20

// 3. Use the correct Math method to get the square root of 9:

let squareRoot = Math.sqrt(9);
console.log(squareRoot); // Output: 3


// Soluation-10

// 1. Choose the correct comparison operator to alert true, when x is greater than y.

let y = 10;
let z = 5;

alert(y > z); // Output: true


// 2. Choose the correct conditional (ternary) operator to alert "Too young" if age is less than 18, otherwise alert "Old enough".

let age1 = 16;

alert(age1 < 18 ? "Too young" : "Old enough"); // Output: "Too young"
